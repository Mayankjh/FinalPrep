# FinalPrep
my first website

step 1 install package.json dependencies also install seperately multer package from npm by typing "npm install multer -s"

step 2 change the database username and password

step 3 app initializes with finalprep.js
